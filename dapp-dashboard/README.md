# DApp Dashboard

Reserved for the premium React/Next.js trading interface.

The browser may connect a wallet for user-facing interactions, but private keys must never be stored, requested, or executed by frontend code. Transaction signing remains outside the dashboard execution architecture until an explicit future authorization boundary is satisfied.

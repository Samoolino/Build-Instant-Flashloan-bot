#!/usr/bin/env node
import http from "node:http";
const port=Number(process.env.PORT||10000);
const allow=process.env.HERMES_CONTROL_TOKEN||"";
const state={mode:process.env.HERMES_MODE||"IMPLEMENTATION",authorization:0,signing:false,broadcast:false,liveExecution:false};
function auth(req){if(!allow)return false;return req.headers.authorization===`Bearer ${allow}`}
function send(res,status,body){res.writeHead(status,{"content-type":"application/json","cache-control":"no-store"});res.end(JSON.stringify(body))}
const html=()=>`<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>Hermes Control</title><style>body{font-family:system-ui;margin:0;background:#0b1020;color:#e8edf7}header{padding:18px 24px;background:#121a32}main{display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));gap:14px;padding:20px}.card{background:#141d37;border:1px solid #2a375b;border-radius:12px;padding:16px}.ok{font-weight:700}.mono{font-family:ui-monospace,monospace}.warn{opacity:.8}</style></head><body><header><h1>HERMES · FLASH BOT CONTROL</h1><div class="mono">Agentic DevOps / Implementation Console</div></header><main><section class="card"><h2>Command</h2><p>Mode: <b id="mode">IMPLEMENTATION</b></p><p>Authorization: <b id="auth">0</b></p><button onclick="refresh()">Refresh</button></section><section class="card"><h2>Execution Gate</h2><p>Signing: <b id="sign">OFF</b></p><p>Broadcast: <b id="broadcast">OFF</b></p><p>Live execution: <b id="live">OFF</b></p></section><section class="card"><h2>Engagement Pipeline</h2><p>Observe → Quote → Simulate → Audit → Lock → Unsigned Intent</p><p class="warn">Final signing/broadcast remains outside Hermes.</p></section><section class="card"><h2>Wallet</h2><p>Connected address: <span id="wallet">none</span></p><button onclick="connectWallet()">Connect wallet</button><p class="warn">Browser wallet connection exposes only the public address.</p></section><section class="card"><h2>Operations</h2><p>RPC observation · tests · deployment · logs · GitHub implementation</p><p class="mono">Credentials are never rendered.</p></section></main><script>async function connectWallet(){const p=window.ethereum;if(!p){document.getElementById("wallet").textContent="no provider";return}try{const a=await p.request({method:"eth_requestAccounts"});document.getElementById("wallet").textContent=a?.[0]||"none"}catch(e){document.getElementById("wallet").textContent="connection rejected"}} async function refresh(){const r=await fetch("/api/state");const s=await r.json();for(const [id,v] of [["mode",s.mode],["auth",s.authorization],["sign",s.signing?"ON":"OFF"],["broadcast",s.broadcast?"ON":"OFF"],["live",s.liveExecution?"ON":"OFF"]])document.getElementById(id).textContent=v}refresh();setInterval(refresh,5000)</script></body></html>`;
http.createServer((req,res)=>{if(req.url==="/healthz")return send(res,200,{ok:true,service:"hermes-control"});if(req.url==="/api/state"){if(!auth(req))return send(res,401,{error:"unauthorized"});return send(res,200,state)}if(req.url==="/"||req.url==="/index.html"){res.writeHead(200,{"content-type":"text/html","cache-control":"no-store"});return res.end(html())}send(res,404,{error:"not_found"})}).listen(port,"0.0.0.0",()=>console.log(`Hermes control UI listening on 0.0.0.0:${port}`));